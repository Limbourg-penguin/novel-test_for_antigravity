# 可愛風格小說網站實作計畫 (Cute Novel Website)

我們將為您打造一個**風格極度可愛、充滿特色**且專注於 **PC 版型** 的小說網站首頁。

## 設計理念 (Design System)

為了達成「可愛」與「特色」，我們將採用以下設計風格：
1. **色彩計畫 (Color Palette)**：使用柔和的馬卡龍色系 / 粉彩色系 (Pastels)，例如櫻花粉、薄荷綠、寶寶藍、奶油黃。背景以米白色或淺粉色為主，避免死白。
2. **字體 (Typography)**：採用圓潤、活潑的字體（例如 Google Fonts 的 `Nunito` 或 `Quicksand`），讓文字看起來親切可愛。
3. **視覺元素 (Visual Elements)**：
   - 大量使用圓角 (Border Radius) 設計。
   - 柔和的陰影 (Soft drop shadows) 營造層次感。
   - 加入微動畫 (Micro-animations)，例如按鈕懸浮時的彈跳效果 (bouncy hover effects)、首頁元素的輕微漂浮感。
   - 搭配可愛的吉祥物與插圖（將使用 AI 圖像生成工具製作）。

## 開發架構

- **框架**：使用 Vite + React 建立前端專案，輕量且快速。
- **樣式**：使用 Vanilla CSS (純 CSS)，以便我們能完全掌控客製化的可愛風格與動畫，不依賴預設框架，打造獨一無二的視覺體驗。
- **圖庫/素材**：缺乏的素材將使用影像生成工具產生可愛風格的插圖 (Logo, Banner, 小說封面示意圖等)。

## 首頁版面規劃 (PC 優先)

1. **頂部導覽列 (Header)**：包含可愛的吉祥物 Logo、網站名稱、以及導覽連結（首頁、分類、排行），並帶有玻璃透視感 (Glassmorphism) 或毛玻璃效果。
2. **橫幅焦點區 (Hero Section)**：展示網站的主打插圖與歡迎標語，背景為夢幻的粉彩漸層，搭配漂浮的書本動畫。
3. **推薦區塊 (Featured Novels)**：以卡片 (Cards) 形式展示小說。卡片會有圓潤的邊角、可愛的封面圖片，滑鼠游標移過去時會有卡片浮起的動畫。
4. **頁尾 (Footer)**：簡單的版權宣告與底部連結，搭配波浪狀的邊緣設計分隔上方內容。

## 實作步驟 (Proposed Changes)

1. **環境建置**：初始化 `Vite` + `React` 專案。
2. **生成素材**：生成可愛吉祥物 Logo、橫幅背景、以及幾張可愛風格的小說封面。
3. **全域樣式 (Global Styles)**：設定 `index.css`，寫入色彩變數 (CSS Variables)、字體引入、全域重置與基礎動畫。
4. **組件開發 (Components)**：
   - `Header.jsx`
   - `HeroBanner.jsx`
   - `NovelGrid.jsx` & `NovelCard.jsx`
   - `Footer.jsx`
5. **組合首頁**：在 `App.jsx` 中整合所有組件。

---

> [!IMPORTANT]
> ## User Review Required
> 請確認您對以上的設計方向（粉彩色系、圓潤風格、React 架構）是否滿意？如果有特別偏好的顏色（例如：希望以藍色系為主，或粉紅色系為主）或者希望在首頁加入其他特定區塊（例如：最新更新清單、排行榜），請讓我知道，確認後我將開始撰寫程式碼並建立專案！
